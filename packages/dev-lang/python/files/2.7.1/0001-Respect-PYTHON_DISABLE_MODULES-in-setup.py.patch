diff -Naur Python-2.7.1.ORIG/setup.py Python-2.7.1/setup.py
--- Python-2.7.1.ORIG/setup.py	2010-12-18 05:39:59.895069897 +0000
+++ Python-2.7.1/setup.py	2010-12-18 05:40:18.256069896 +0000
@@ -21,7 +21,11 @@
 COMPILED_WITH_PYDEBUG = hasattr(sys, 'gettotalrefcount')
 
 # This global variable is used to hold the list of modules to be disabled.
-disabled_module_list = []
+pdm_env = "PYTHON_DISABLE_MODULES"
+if pdm_env in os.environ:
+    disabled_module_list = os.environ[pdm_env].split()
+else:
+    disabled_module_list = []
 
 def add_dir_to_list(dirlist, dir):
     """Add the directory 'dir' to the list 'dirlist' (at the front) if
